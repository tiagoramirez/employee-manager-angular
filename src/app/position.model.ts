export class Position {
    constructor(name: string, salary: number, floor: number) {
        this.name = name;
        this.salary = salary;
        this.floor = floor;
    }

    name: string;
    salary: number;
    floor: number;
}